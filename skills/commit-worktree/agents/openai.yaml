interface:
  display_name: "Commit Worktree"
  short_description: "按 type(scope): message 规范整理并提交当前工作区改动"
  default_prompt: "Use $commit-worktree to inspect the current worktree, compose a conventional commit message, and commit the relevant changes."
