interface:
  display_name: "结构化代码重构"
  short_description: "按指定或排除目录扫描职责边界，拆分大文件并整理代码库结构"
  default_prompt: "使用 $refactor-codebase-structure 检查并重构当前代码库或我指定目录的职责、复用边界和目录分层。"

policy:
  allow_implicit_invocation: false

platform:
  unix: true
  windows: true
